PORT=3000
# Optional subpath for mounting under a larger site (e.g., /ENL3Final)
BASE_PATH=/ENL3Final
# Override API paths if your WAF blocks the defaults
RESULTS_ENDPOINT=/api/results
HEALTH_ENDPOINT=/api/health
# Password required to read stored results
RESULTS_PASSWORD=Happy
